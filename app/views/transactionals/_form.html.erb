<%= form_for(@transactional) do |f| %>
  <% if @transactional.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@transactional.errors.count, "error") %> prohibited this transactional from being saved:</h2>

      <ul>
      <% @transactional.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <%= hidden_field :campaign , @parent_campaign %>
  <div class="field">
    <%= f.label :xml %><br />
    <%= f.text_area :xml %>
  </div>
  <div class="field">
    <%= f.label :shell %><br />
    <%= f.text_area :shell %>
  </div>
   <div class="field">
    <%= f.label :folder %><br /> 
    <%= f.file_field :folder %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
